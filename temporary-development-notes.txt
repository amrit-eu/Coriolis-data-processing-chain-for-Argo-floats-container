

The docker compose needs two images

The Matlab runtime - this is pulled from Docker hub
The decoder image - This is built from the Dockerfile


1. Build the image
docker build -t float-decoder -f Dockerfile . 


2. Clear out the files in output (iridium & nc)


3. Run it all together
docker compose -f .\docker-compose.yml up -d --build


-----------------------------